

export interface StepsAttrs {
  active?: number | string,
  direction?: 'horizontal' | 'vertical',
  activeIcon?: string,
  inactiveIcon?: string,
  finishIcon?: string,
  activeColor?: string,
  inactiveColor?: string,
  name?: string
}