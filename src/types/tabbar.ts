
export interface TabbarAttrs {
  bottom?: boolean,
  inactiveColor?: string,
  activeColor?: string,
  route?: boolean,
  fixed?: boolean,
  border?: boolean,
  placeholder?: boolean,
  safeAreaInsetBottom?: boolean,
  name?: string,
  icon?: string,
  href?: string,
  to?: string,
  badge?: string
  dot?: boolean,
  replace?: boolean
}